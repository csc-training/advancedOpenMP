# multiple sections of the same array (section in map clause)



Map required data for the target region.
map y tofrom, and only sections of data required for
x (1st quarter of array, and last half).
Compile and run `mult_array_section(.cpp|.F90)` 

1.) Look over the code.  
    Map appropriate array sections of x
    Compile code and run.

```
./a.out
```
